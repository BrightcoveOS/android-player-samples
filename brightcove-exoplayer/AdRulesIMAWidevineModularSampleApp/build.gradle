android {
  defaultConfig.applicationId = "com.brightcove.player.samples.exoplayer.imawidevine.adrules"

    defaultConfig {
        minSdkVersion ANDROID_MIN_SDK_VERSION
        targetSdkVersion ANDROID_TARGET_SDK_VERSION
        multiDexEnabled true
    }
}